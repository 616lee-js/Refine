// TODO (Phase 3): Implement prompt testing CLI
//
// Purpose: developer tool to test draft prompt edits before applying them.
// Takes a draft prompt file path + fixture inputs, runs through orchestrator,
// writes results to a reviewable output file. Not an app feature.
//
// Rough interface (to be designed in Phase 3):
//   npx tsx scripts/test-prompt.ts --prompt src/lib/layer2/draft.md --fixtures scripts/fixtures/test-inputs.json
